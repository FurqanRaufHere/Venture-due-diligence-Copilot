services:
  - type: web
    name: ai-vdd-copilot
    env: python
    buildCommand: pip install -r requirements.txt && python -m utils.startup_dataset
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false